# GUI-local ignores (scoped to iroc_gui/)
__pycache__/
*.py[cod]
.venv/
venv/
.DS_Store

# Local runtime config (machine-specific paths/IPs/token). config.default.json
# is the committed template; config.json is written by the GUI on first save.
config.json
